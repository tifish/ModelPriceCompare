interface:
  display_name: "Model Price Compare"
  short_description: "Compare LLM token prices and add newly listed priced models."
  default_prompt: "Compare token prices for these models using official pricing pages. Include cached input, uncached input, output, multipliers, source URLs, and add newly listed eligible models found on pricing pages."
